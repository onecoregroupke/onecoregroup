# Claude Code Notes

Inspect manifests first, then repair the smallest broken module. Preserve the CLI contract and folder structure. Do not replace the code-native rendering pipeline with design tools or AI video tools.

For creative tasks, create or update plans and scene source files, then use the renderer/compiler modules. For delivery, prefer local Drive sync unless the user explicitly asks for Hermes API upload.
