# Agent Contract

- Operate through `node src/cli.mjs`.
- Keep every task in `projects/TASK-XXXX`.
- Read and write manifests; do not invent delivery status.
- Use code-native visuals only.
- Validate audio and licensing metadata before compile.
- Deliver drafts only.
- Never publish, message clients, or mark `Completed`.
- Log generated FFmpeg commands and delivery actions.
- Report missing assets with exact paths the user should fill.
