// Re-export all shared UI primitives
// Each app also has its own app-specific components
// This package contains truly cross-app primitives

export { Button } from './Button'
export { Badge } from './Badge'
